import { NgxAutocomPlaceDirective } from './ngx-autocom-place.directive';

describe('NgxAutocomPlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxAutocomPlaceDirective();
    expect(directive).toBeTruthy();
  });
});
